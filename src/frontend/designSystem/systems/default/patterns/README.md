# Default Pattern Proof Routes

This folder is for rendered Layer 4 pattern proof routes for the `default`
design system.

Do not treat legacy top-level `/design-system/patterns/*` route markup as a
governed Layer 4 proof. New governed pattern proofs should live under:

```text
src/frontend/designSystem/systems/default/patterns/<pattern-name>/
```
