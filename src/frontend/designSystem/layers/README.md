# Governed Design-System Layers

This directory contains runtime seams that belong to the numbered frontend
harness.

Use this area for new governed Layer 2+ source seams when the corresponding
docs under `docs/design-system/` have passed their layer gates.

Current active source layers:

```text
02-token/
03-primitive/
```

Top-level folders such as `assets/`, `tokens/`, `components/`, `patterns/`,
and `canonicals/` contain earlier design-system inventory unless a governed
artifact explicitly promotes a seam into this numbered layer structure.
