# Layer 4 Pattern Contract Runtime Seams

This folder is for governed Layer 4 pattern runtime seams.

Do not add a pattern runtime seam here until the corresponding shared pattern
contract and required system proof exist under `docs/design-system/04-pattern-contract/`.

Legacy top-level `src/frontend/designSystem/patterns/` routes are review
inventory unless explicitly promoted through Layer 4.
