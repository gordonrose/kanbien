export const tooltipSurfaceTokenContract = {
  schema: "kanbien.designSystem.tokenContract.v1",
  contractId: "tokens.tooltip-surface",
  tokenType: "tooltip-surface",
  requiredVariantRoles: ["text overflow disclosure surface"],
  requiredThemes: ["original", "dark", "desert"],
  requiredVariantFields: ["id", "tokenName", "value", "preview", "metadata", "useCaseInstructions"],
  valueFields: [
    "surfaceRole",
    "backgroundValue",
    "foregroundValue",
    "borderValue",
    "shadowValue",
    "radiusValue",
    "paddingBlockValue",
    "paddingInlineValue",
    "maxInlineSizeValue",
    "zIndexValue",
    "motionDurationValue",
    "motionEasingValue",
  ],
  metadataFields: ["surfaceRole", "theme", "state", "accessibility"],
  useCaseInstructionFields: ["allowedUse", "forbiddenUse", "accessibilityRule"],
  consumerRules: [
    "Consumers must use this token for governed full-text disclosure surfaces instead of local tooltip surface, border, shadow, spacing, z-index, or motion literals.",
    "This token does not define tooltip trigger markup, positioning, hover, focus, touch, dismissal, ARIA behavior, or copy/select behavior.",
    "Text-overflow disclosure primitives must preserve keyboard and full-text access behavior from the behavior rule before claiming readiness.",
  ],
};
