# Workspace Exports

This folder contains generated workspace export artifacts.

Current artifact:

- `capability-contract-catalog-v1.generated.json`

The capability contract catalog export is generated by the
`capabilityContractCatalog` feature. Its default output path is owned by
`src/features/capabilityContractCatalog/generation/artifact.ts` and may be
overridden with `CAPABILITY_CATALOG_ARTIFACT_PATH` for tests or controlled
alternate output.

Do not hand-edit this export as source truth. Refresh it through the
capability contract catalog materialization/export flow, then review the
generated diff.

Repo bucket classification: `shared-governance-kernel`.

Move caution: do not move this folder or artifact without updating the
generator default path, API and feature documentation, and any tests or
operator workflow that rely on the current path.
