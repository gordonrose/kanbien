# Moved: Harness Platform Boundary Map

This exploratory harness-platform boundary map was moved out of active
`harness-audits/` because its long-term owner is genuinely undecided. It is not
ordinary archive history, and it should not be treated as current harness law.

New location:

- `docs/workspace-buckets/unsure-needs-decision/harness-platform-boundary-map/2026-05-05-harness-platform-boundary-map.md`

Decision needed before promotion:

- Should this become a `shared-governance-kernel` reusable harness-platform
  boundary artifact?
- Should it become a `discovery-harness` Product Discovery or Technical
  Steering packet for a future reusable harness product?
- Or should it be archived as an exploratory draft after its useful decisions
  are extracted?

This breadcrumb is temporary compatibility support for older references. It
should be removed during the final docs cleanup sweep once upstream references
have been updated or confirmed obsolete.
