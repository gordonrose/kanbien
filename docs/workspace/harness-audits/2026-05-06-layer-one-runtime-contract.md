# Layer One Runtime Contract Moved

This file is a breadcrumb for the Layer One Runtime Contract that previously
lived in `docs/workspace/harness-audits/`.

Use the canonical contract instead:

- `docs/product-discovery/layer-one-runtime-contract.md`

Use the runtime behavior instructions here:

- `.codex/skills/20-planning-artifacts/product-discovery-maintainer/SKILL.md`

Status:

- moved
- retained only so old references have a clear redirect trail

Why:

- `docs/workspace/harness-audits/` is for audit and reconciliation notes, not
  active Layer 1 runtime contracts
- the contract is durable Product Discovery harness guidance
- behavioral runtime rules have been promoted into the Product Discovery skill,
  while the request/response/state reference now lives in durable Product
  Discovery docs

Compatibility note:

- old chats or docs may still point to this path
- follow `docs/product-discovery/layer-one-runtime-contract.md` for current
  contract reference
- follow the Product Discovery skill for assistant/runtime behavior
- this breadcrumb can be deleted only after maintained references are updated
  and old path compatibility is no longer intentionally supported
