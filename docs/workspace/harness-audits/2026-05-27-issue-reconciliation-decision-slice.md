# Issue-Reconciliation Decision Slice

Date: `2026-05-27`

Status: deferred comparison slice.

Purpose: preserve the first 25 cleanup decisions as a future comparison queue
for after the new frontend source of truth has matured through the
`41-front-end` harness.

This slice makes no approval claims and does not request immediate decisions.
The remaining older frontend reconciliation notes should be treated as
reference and reconciliation material while the frontend is rebuilt through the
new harness. They are not current construction APIs and should not override
new `41-front-end` behavior rules, token definitions, primitives, pattern
contracts, or later governed adoption seams.

Do not archive these records merely because the old frontend appears fixed.
Return to this slice after the new frontend harness source of truth is mature
enough to compare each old lesson against current governed artifacts. At that
point, classify each record as:

- `reference-only`: useful history, no longer active truth
- `promote-to-harness`: a lesson that still needs a behavior rule, token,
  primitive, pattern, component, or adoption seam
- `active-blocker`: a current issue that still blocks safe frontend migration
- `runtime-process-caveat`: a verification, server, workstation, or workflow
  lesson that remains relevant outside the design-system source of truth

## Deferred Comparison Queue

| # | File name or names | Topic | Description | Why a decision is needed | Risk of getting it wrong | Recommended decision |
|---|---|---|---|---|---|---|
| 1 | `2026-04-14-accessibility-menu-rtl-anchor.md`, `2026-04-14-navigation-tooltip-rtl-anchor.md` | RTL rail-attached overlay policy | These two records define direction-aware anchoring expectations for rail menus and tooltips in RTL. | Decide whether the current RTL overlay lesson is now promoted enough to archive both records together. | Archiving too early could lose a live warning for future rail overlays; keeping them active too long makes the active folder overstate old fixed regressions. | Archive both as one promoted RTL overlay lesson if current RTL open-state visual coverage is still in place. |
| 2 | `2026-04-15-root-admin-browser-helper-guidance-regression.md` | Browser auth helper UX | The root-admin SSH verification stage needs a visible helper launch/download recovery path. | This has a workstation UX component that may need human confirmation, not just source inspection. | Archiving without real UX confirmation could hide a broken login recovery path; keeping it active may block cleanup even if the helper UX is acceptable. | Keep active until Gordon confirms the helper guidance is visible and useful in the real browser login flow. |
| 3 | `2026-04-15-root-admin-browser-signer-windows-key-permissions-regression.md` | Windows signer launcher proof | The helper launcher stages a Windows-mounted SSH key into WSL with locked-down permissions. | Decide whether desktop-level confirmation has happened and is sufficient to archive the record. | Archiving too early could hide a root-admin login blocker on the actual workstation; over-testing could turn a local environment issue into permanent product policy. | Keep active until the desktop shortcut has been confirmed to start the helper successfully on the real workstation. |
| 4 | `2026-04-16-shell-scroll-attachment-drift.md` | Shell scroll attachment parity | The design-system shell and root-admin shell need sticky/fixed chrome to remain attached during scroll. | Decide whether first-consumer root-admin parity proof is complete enough to archive this. | Archiving too early could allow shell scroll drift to reappear in app pages; keeping it active may duplicate existing shell visual coverage. | Keep active until root-admin first-consumer scroll attachment parity is explicitly verified or referenced from current app-shell tests. |
| 5 | `2026-04-20-governed-frontend-adoption-component-seam-gap.md` | Governed adoption seam policy | Shared CSS alone is insufficient; governed app adoption needs design-system-owned styling, render, controller, and accessibility seams. | Decide whether this remains an active governance warning or becomes a backlog parent for remaining migrations. | Archiving this before remaining app-local seams are resolved would weaken the strongest adoption guardrail; leaving it vague makes future work hard to route. | Keep active as the parent governance item until remaining app-local governed seams are either migrated or explicitly excepted. |
| 6 | `2026-04-20-root-admin-hierarchy-current-page-selection-drift.md` | Hierarchy initial selection | The web-app hierarchy editor must bind the page-settings form to the current route, not the first tree node. | Decide whether current coverage is enough to archive this as a promoted root-admin hierarchy lesson. | Archiving too early could miss wrong-page mutation regressions; keeping it active may duplicate a focused browser regression. | Archive after one quick confirmation that the mixed-tree browser regression still asserts current-route selection. |
| 7 | `2026-04-20-root-admin-hierarchy-inline-open-actions-incomplete.md` | Hierarchy inline action visibility | The hierarchy tree inline open action existed in the DOM but rendered invisibly because icon geometry collapsed. | Decide whether this is now fully covered by row-taxonomy and rendered-geometry tests. | Archiving too early could leave invisible action regressions uncovered across row types; keeping it active may be stale if geometry assertions are durable. | Archive if current hierarchy visual tests still cover rendered action geometry across root-family and page rows. |
| 8 | `2026-04-20-root-admin-hierarchy-rail-ownership-conflict.md` | Hierarchy rail ownership | The page needed a way to choose hierarchy drawer default-open behavior without fighting shell rail ownership. | Decide whether this remains a live architecture warning or is now a promoted consumer-option lesson. | A wrong call could reintroduce app-local ownership conflicts around governed drawers; over-retaining it could duplicate the hierarchy-tree consumer contract. | Archive if the shared hierarchy-tree consumer still owns `initialDrawerOpen` or equivalent page-owned default posture. |
| 9 | `2026-04-20-root-admin-hierarchy-refresh-discovery-reconcile-gap.md` | Hierarchy discovery refresh | Discovery/apply behavior needed reconciliation so refreshed hierarchy data did not drift from current route truth. | Decide whether current topology/discovery governance has absorbed the lesson. | Archiving too soon could hide route-discovery drift; keeping it active may duplicate topology materialization coverage. | Keep active until the relevant topology materialization or hierarchy discovery artifact is identified as the current authority. |
| 10 | `2026-04-20-root-admin-hierarchy-structural-action-parity-gaps.md` | Hierarchy structural action parity | Hierarchy actions needed parity across different row types and structural nodes. | Decide whether action parity now belongs in tests only or remains a live product-review warning. | If archived prematurely, some row classes may silently lose affordances; if kept active without scope, it becomes a vague warning. | Keep active unless current visual/browser coverage proves action presence, icon geometry, and target behavior across the full visible row taxonomy. |
| 11 | `2026-04-20-root-admin-page-settings-form-template-adoption-drift.md` | Page-settings form-template adoption | Root-admin page settings was using or approximating form-template behavior in an app-local way. | Decide whether the remaining gap is resolved, accepted as an exception, or part of the governed adoption backlog. | Archiving without a seam decision would bless local governed form reconstruction; keeping it active without an owner will keep recurring. | Keep active and attach it to the governed adoption backlog unless the current implementation consumes the signed-off form-template seam end to end. |
| 12 | `2026-04-20-root-admin-page-settings-icon-default-mismatch.md` | Page-settings icon default | The page-settings editor had an icon default mismatch between stored/default state and visible UI. | Decide whether current icon-grid/shared default behavior has promoted the lesson. | Wrongly archiving could let new page records show misleading icon defaults; keeping it active may duplicate icon-grid adoption coverage. | Archive if current root-admin page-settings tests prove default icon projection and saved icon persistence through the DS-owned icon-grid seam. |
| 13 | `2026-04-20-root-admin-page-settings-save-silent-failure.md` | Page-settings save feedback | Save failure could be silent or appear successful despite not producing the intended durable effect. | Decide whether the active contract is save transport success, visible shell update, or both. | Getting this wrong can make operators trust a save that did not affect the live shell; over-broad handling can create noisy error UX. | Keep active until save success is explicitly defined as durable persistence plus visible projection refresh for the affected shell setting. |
| 14 | `2026-04-21-root-admin-breadcrumb-pressure-drift.md` | Root-admin breadcrumb pressure | Breadcrumb layout could break under pressure in the root-admin shell. | Decide whether this is covered by current sub-nav/breadcrumb visual tests or still needs root-admin-specific proof. | Archiving too early could miss app-shell overflow even if design-system breadcrumb tests pass; keeping it active may duplicate sub-nav coverage. | Archive only if root-admin shell tests prove breadcrumb pressure behavior in the real app frame, not just the design-system canonical. |
| 15 | `2026-04-21-root-admin-breadcrumb-tooltip-on-load.md` | Breadcrumb tooltip load state | Breadcrumb tooltip behavior could appear incorrectly on initial load. | Decide whether this is a promoted sub-nav lesson or still a root-admin runtime caveat. | Wrong archival could leave initial-load hover/focus artifacts uncaught; over-retention may keep an already-fixed UI timing issue active. | Archive if root-admin app tests cover clean initial breadcrumb state plus tooltip behavior after intentional hover/focus. |
| 16 | `2026-04-21-root-admin-hierarchy-drawer-dismissal-gap.md` | Hierarchy drawer dismissal | Drawer dismissal behavior needed to respect signed-off hierarchy drawer interaction rules. | Decide whether current shared drawer/controller seams fully own dismissal semantics. | Getting this wrong could reopen outside-click, escape-key, or coexistence regressions in the app shell. | Keep active until the hierarchy drawer dismissal rules are traced to a current shared controller and app visual coverage. |
| 17 | `2026-04-21-root-admin-paired-drawers-regression.md` | Paired drawer coexistence | Display settings and hierarchy drawer must be able to remain open side by side on desktop. | Decide whether paired-drawer coexistence is an enduring shell rule or a route-local exception. | A bad decision could make future panels close each other unexpectedly or create uncontrolled overlay stacking. | Promote as an enduring governed shell coexistence rule, then archive once the shared keep-open seam and app regression are verified. |
| 18 | `2026-04-21-root-admin-root-users-sr-only-grid-drift.md` | Hidden helper class layout risk | Stale `sr-only` helpers participated in CSS grid and broke the root-users split layout. | Decide whether this lesson has been absorbed into static guards or should remain active. | Archiving too soon could let legacy hidden classes re-enter governed layouts; keeping it active may duplicate static guard coverage. | Archive if static governance and root-users visual geometry tests reject stale hidden helpers and prove lane alignment. |
| 19 | `2026-04-21-root-admin-top-nav-page-settings-unwired.md` | Saved page setting drives top nav | `showInTopNav` must update the real root-admin top nav, not only persist in the backend. | Decide whether this stays active as a cross-feature projection warning or can archive as covered behavior. | Wrong archival could hide durable-setting projection regressions; over-retention may duplicate browser tests. | Archive if browser coverage still toggles the setting, verifies immediate top-nav refresh, reloads, and verifies persistence. |
| 20 | `2026-04-21-web-app-hierarchy-module-route-landing-page-gap.md` | Module route landing pages | Discovery must create selectable module-root pages when a route is both navigable and a parent prefix. | Decide whether this is now a promoted topology materialization lesson. | Archiving too early could lose index-route support in hierarchy selectors; keeping active may duplicate discovery/apply tests. | Archive if discovery/apply tests still cover a module route with child routes, such as `/design-system/canonicals`. |
| 21 | `2026-04-22-canonical-renderings-visible-worktree-verification-gap.md` | Visible workspace verification | Verification in an isolated worktree was mistaken for verification in the workspace the user could inspect. | Decide whether to keep this as a live process warning or move it into workflow guardrails. | Archiving without workflow promotion could repeat false "done" claims; leaving it active forever duplicates branch/worktree rules. | Promote the lesson into workflow guardrails or chat-bootstrap guidance, then archive the note. |
| 22 | `2026-04-22-root-admin-users-workspace-seam-correction.md` | Root-users workspace seam | Root-users had to move list-page render and controller ownership into a DS-owned seam. | Decide whether this correction is complete enough to archive. | Archiving too soon could mask app-local list-page reconstruction; keeping it active may duplicate the governed-root-admin static guard. | Archive if `check:governed-root-admin-ui` still blocks app-local root-users host markup and visual tests cover the DS seam. |
| 23 | `2026-04-22-root-admin-web-app-hierarchy-host-seam-correction.md` | Web-app hierarchy host seam | Web-app hierarchy still owned governed host markup locally after sharing controller logic. | Decide whether this is resolved or remains a live app-adoption warning. | Wrong archival could bless shared-controller-only adoption, which the repo now rejects; over-retention may duplicate static guard evidence. | Archive if the DS-owned render seam remains the only host source and static governance blocks app HTML reintroduction. |
| 24 | `2026-04-24-brochure-preview-stale-server.md` | Manual preview server freshness | A deleted-worktree preview server served stale/error output on the manually shared localhost port. | Decide whether this belongs as a standing runtime verification rule or can archive as a one-off process incident. | Archiving without a process rule could repeat stale-localhost confusion; over-retention may make a local process cleanup look like product debt. | Promote the stale-server check into runtime handoff guidance, then archive this record. |
| 25 | `2026-04-24-date-time-canonical-ready-before-reserve.md` | Date/time canonical readiness threshold | A date/time canonical was treated as ready before its required reservation or readiness gate was truly satisfied. | Decide whether this is a live governance caveat for canonical readiness or already covered by the design-system loop. | Archiving too early could allow premature ready/signoff language; keeping it active may duplicate current canonical readiness checks. | Keep active until the exact readiness/reservation authority is identified and shown to block premature ready language. |

## Boundary After These 25

The next unresolved records after this slice are mostly newer frontend and app
surface decisions: top-nav first-paint and dark-theme user-review caveats,
list-form drawer scrollbar behavior, root-admin directory/login/context-nav
records, build backlog/chat workspace proof routes, entity management
compression, and count-card route alias fallback.

## Current Handling

Until the frontend rebuild reaches a mature governed baseline, keep the
remaining active frontend issue-reconciliation records available as reference
material. Use them to seed layer-work preflight, behavior-rule discussions,
token and primitive decisions, and later adoption checks, but do not treat
their old implementation details as source-of-truth instructions for the new
frontend.
