# Product Request Backlog Model Moved

This file is a breadcrumb for the older Product Request backlog model note that
previously lived in `docs/workspace/harness-audits/`.

Use the maintained Product Request guidance instead:

- `docs/workspace/product-requests/README.md`

Use the maintained Product Request template here:

- `docs/templates/product-request-template.md`

Status:

- superseded
- retained only so old references have a clear redirect trail

Why:

- `docs/workspace/harness-audits/` is for audit and reconciliation notes, not
  long-term Product Request model truth
- durable Product Request ownership, lifecycle, routing, evidence, approval,
  and first-version boundary guidance has been promoted into
  `docs/workspace/product-requests/README.md`
- the maintained template under `docs/templates/` remains the source for new
  Product Request cover sheets

Compatibility note:

- existing Product Request files may still contain `Related model` links to
  this path
- follow `docs/workspace/product-requests/README.md` for current model rules
- follow `docs/templates/product-request-template.md` for new request shape
- this breadcrumb can be deleted only after maintained references are updated
  and old path compatibility is no longer intentionally supported
