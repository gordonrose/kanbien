# Colours Canonical Rendering Exception

Approved canonical rendering exception.

`colours` may use `/design-system/tokens/colours` as its signed-off review
surface for this promotion pass. Generated canonical render pages should be
added if colour decisions are split into separately governed component states.
