# Design System Component Inventory

## Purpose

Track the current `/design-system` surfaces that have been explored and, where
applicable, signed off.

This inventory is the promotion ledger for moving from design-system proof to
governed app adoption.

## Status Legend

- `exploratory`: implemented on `/design-system`, still iterating
- `signed-off`: accepted on `/design-system` by the user
- `system-ready`: documented and verified enough to begin app adoption
- `adopted`: in real app use through an intentional seam
- `needs-review`: drift, regression, or missing-state risk blocks promotion

## Assumption For Initial Population

This initial pass assumes the current `/design-system` components that the user
has already approved should be treated as `signed-off` for the demonstrated
surface unless a later review moves them back to `needs-review`.

## Inventory

| Family | Public parent | Artifact | Current status | Signed-off scope | Next gate to clear | First adoption target |
| --- | --- | --- | --- | --- | --- | --- |
| `top-nav` | `components` | Brand lockup and desktop top navigation shell | `adopted` | `/design-system` desktop shell plus the `/root-admin` POC authenticated shell | App-vs-reference parity review and follow-up extraction of a shared seam if the second consumer confirms the API | Root admin shell header |
| `top-nav` | `components` | Primary navigation overflow behavior | `adopted` | `/design-system` responsive header plus the `/root-admin` POC authenticated shell | App-vs-reference parity review at root-admin widths and long-label states | Root admin shell header |
| `menu` | `components` | Desktop profile menu | `adopted` | `/design-system` top-nav profile affordance plus `/root-admin` account menu | Broaden parity checks and document persistence expectations before a second consumer | Root admin shell account menu |
| `dialog` | `components` | Language selector modal | `adopted` | `/design-system` profile preference interaction plus `/root-admin` language selector POC | Add persistence and richer accessibility requirements before wider reuse | Root admin shell account preferences |
| `selection-list` | `components` | Language option list | `adopted` | `/design-system` language selection flow plus `/root-admin` language selector POC | Confirm persistence contract and second-consumer semantics before shared preference rollout | Shared preference selectors |
| `top-nav` | `components` | Mobile primary navigation and mobile profile submenu | `adopted` | `/design-system` narrow-width header plus the `/root-admin` POC authenticated shell | App-vs-reference parity review for mobile and overflow thresholds | Root admin shell mobile header |
| `sub-nav` | `patterns` | Breadcrumb and search shared secondary row composition | `adopted` | `/design-system` page search and breadcrumb row plus `rootAdminShell` page chrome | Draft component artifact now exists; next higher-confidence gate is second-consumer validation before shared code extraction | Root admin shell page chrome |
| `breadcrumb` | `patterns` | Breadcrumb row with collapse and compact modes | `adopted` | `/design-system` sub-nav breadcrumb plus `rootAdminShell` page chrome | Add real-content long-label parity states and confirm tooltip/truncation behavior inside the real consumer | Root admin shell page chrome |
| `search-shell` | `patterns` | Header search input shell | `adopted` | `/design-system` sub-nav search plus `rootAdminShell` page chrome | Validate a second governed consumer in the shared application header and broaden app-vs-reference parity checks | Root admin shell page chrome |
| `context-nav` | `patterns` | Vertical context rail and bottom-nav responsive conversion | `system-ready` | Signed-off `CNR-001` through `CNR-010` canonical set on `/design-system` | First-consumer parity review in `rootAdminShell` against the signed-off `CNR-*` states | Root admin shell section navigation |
| `launcher` | `templates` | Canonical launcher-page framing and direct-link review grid | `system-ready` | Signed-off launcher-template reference pack on `/design-system/templates/launcher`, legacy `/design-system/canonicals` launchers, and persistence-backed generated family launchers under `/design-system/canonical-renderings/*` | Keep generated launcher publication coupled to persisted registry truth and per-family render-surface proof; no dedicated behavior lock is required unless launcher semantics grow beyond the conventions-driven template contract | Governed review entry pages |
| `canonical-render-page` | `templates` | Focused one-state canonical rendering review page | `system-ready` | Signed-off canonical-render-page reference pack on `/design-system/templates/canonical-render-page`, component/pattern render pages, and generated render routes under `/design-system/canonical-renderings/:familyKey/:referenceId` | Keep generated render routes coupled to registered render surfaces, specimen markers, fallback absence, theme scope, responsive width, and overlay containment proof | Governed canonical rendering pages |
| `page-shell-banner` | `patterns` | Shell-owned banner feedback zone for governed page shells | `adopted` | Signed-off template-hosted banner review batch on `/design-system/templates/page-shell` with informational, success, warning, and danger states plus dismiss controls, now backed by a shared design-system render/controller seam, dedicated canonicals, a component artifact, and a first real consumer in `rootAdminShell` | Prove the same runtime seam in a second governed shell consumer before broadening the shared API | Root admin shell page-shell feedback |
| `page-background` | `tokens` | Global environmental background foundation for design-system shells and structural primitives | `system-ready` | Background token editor at `/design-system/tokens/background`, now promoted into the shared `PageBackground` controller seam with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First app consumer must prove parity against the page-background reference pack; generated canonical states remain deferred by the token-route canonical exception | Shared design-system shell and future page structures |
| `page-template` | `templates` | Reusable page-shape catalog for list, content, table, builder, form, and record-management surfaces | `signed-off` | Signed-off `List Page` parent-template chain on `/design-system/templates/list-page`, the codified `Form Template` parent baseline on `/design-system/templates/form`, and the `record_management_list_centric` template demo on `/design-system/templates/record_management_list_centric` | Turn the `Form Template` reference states into canonicals, and keep `record_management_list_centric` as a demo until its behavior lock, reference pack, and verification checklist are approved | Shared app page families |
| `list-page-structure` | `tokens` | Foundation page shell structure for future list-style pages with 24-column first header, configurable secondary header, full/split lower region, and desktop resize seam | `signed-off` | Signed-off `ListPageStructure` token route at `/design-system/tokens/list-page-structure`, backed by a shared controller seam in `src/frontend/designSystem/assets/listPageStructure.mjs`, a behavior lock, reference pack, verification checklist, component artifact, and adoption contract | Convert the reference pack into generated canonical renderings and complete first-consumer parity proof before claiming app adoption | Future list-style page families |
| `entity-page-structure` | `tokens` | Foundation entity record page structure with shared 24-column header, two-column navigation index, and ten-column record panel | `system-ready` | Signed-off `EntityPageStructure` token route at `/design-system/tokens/entity-page-structure`, consuming the shared `PageBackground` seam and shared foundation header controller, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | Complete first-consumer parity proof before claiming app adoption; generated canonical renderings remain deferred by the token-route canonical exception | Future entity record page families |
| `nested-entity-record` | `tokens` | Nested entity record frame with reusable entity record body structure and horizontal/vertical resize handles | `system-ready` | Signed-off token route at `/design-system/tokens/nested-entity-record`, consuming the shared entity record body seam, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | Complete first-consumer parity proof before app adoption; split into richer child artifacts if nested record behavior grows beyond the current frame contract | Future nested entity workflows |
| `filter-panel-structure` | `tokens` | Overlay filter panel foundation with fixed overlay width, sticky 3:1 title section, mobile full-width layout, and non-stretching scroll stack | `system-ready` | Signed-off token route at `/design-system/tokens/filter-panel-structure`, backed by `filterPanelStructure.mjs`, visual scroll/width coverage, its own behavior lock, reference pack, component artifact, and canonical-rendering exception | Complete first-consumer parity proof before collection filter adoption | Future collection and list filter overlays |
| `search-panel` | `tokens` | Overlay search panel foundation with fixed overlay width, sticky 3:1 title section, pinned search row, mobile full-width layout, and scroll-only result stack | `active` | Token route at `/design-system/tokens/search-panel`, backed by `renderSearchPanelStructure` and `hydratePanelStructures` in `filterPanelStructure.mjs`, visual scroll/pinned-row coverage, and a component artifact | Complete behavior lock, reference pack, verification checklist, and first-consumer parity proof before app adoption | Future collection and list search overlays |
| `container` | `tokens` | Reusable outer grouped surface token with background, border, and state-color treatment | `system-ready` | Signed-off token route at `/design-system/tokens/container`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must consume tokenized surface values without app-page CSS redefinition | Shared app surface containers |
| `container-section` | `tokens` | Reusable interior section surface for grouped or repeated regions inside containers | `system-ready` | Signed-off token route at `/design-system/tokens/container-section`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must prove section/container nesting parity without copying token route markup | Shared app container subsections |
| `index-card` | `tokens` | Compact count-backed card seam for indexes, tab summaries, nested object lists, and supporting card groups | `system-ready` | Signed-off token route at `/design-system/tokens/index-card`, backed by `indexCard.mjs`, route/visual tests, a behavior lock, reference pack, component artifact, verification checklist, and adoption contract; secondary-list-card remains a compatibility alias only | First consumer must consume `indexCard.mjs` and prove real-wrapper parity without app-page CSS or copied token-route markup | Count-backed index and tab card surfaces |
| `button-card` | `tokens` | Compact icon-and-label card button seam for launcher choices and supporting action groups | `system-ready` | Token route at `/design-system/tokens/button-card`, backed by `buttonCard.mjs`, route/visual tests, a behavior lock, reference pack, component artifact, verification checklist, and adoption contract | First consumer must consume `buttonCard.mjs` and prove real-wrapper parity without app-page CSS or copied token-route markup | Icon-backed launcher and action-card surfaces |
| `list-card` | `tokens` | Full-row list card seam for title/subtitle/status record previews | `system-ready` | Signed-off token route at `/design-system/tokens/list-card`, backed by `listCard.mjs`, route/visual tests, a behavior lock, reference pack, component artifact, verification checklist, and adoption contract | First consumer must consume `listCard.mjs` and prove real-wrapper parity without app-page CSS, copied token-route markup, or local colour derivations | Record-management and list-centric row cards |
| `colours` | `tokens` | Semantic colour scale review for foundation, text, state, and accent decisions | `system-ready` | Signed-off token route at `/design-system/tokens/colours`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must use semantic variables rather than raw color literals | Shared semantic colour system |
| `paragraph` | `tokens` | Paragraph, label, and status text scale with semantic ink variants | `system-ready` | Signed-off token route at `/design-system/tokens/paragraph`, backed by `tokenParagraphModel.mjs`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must prove long text and status colour parity in its real wrapper | Shared app typography |
| `header` | `tokens` | Header hierarchy scale with theme-aware heading ink | `system-ready` | Signed-off token route at `/design-system/tokens/header`, backed by `tokenHeaderModel.mjs`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must prove heading scale and no-overlap behavior in real page/container context | Shared app heading hierarchy |
| `icon-button` | `tokens` | Icon-only command button frame with approved size bounds, state treatment, accessible-name rule, and tooltip relationship | `system-ready` | Signed-off token route at `/design-system/tokens/icon-button`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must prove accessible name, focus visibility, target size, and tooltip parity | Shared icon-only controls |
| `dropdowns` | `tokens` | Lightweight single-select dropdown token that reuses the signed-off simple-select anchored-listbox runtime | `system-ready` | Token route at `/design-system/tokens/dropdowns`, with `simple-dropdown` and `simple-select` token compatibility aliases, route/visual tests, a behavior lock, reference pack, component artifact, verification checklist, and canonical-rendering exception | First consumer must reuse the shared simple-select runtime and prove trigger/listbox parity without copying token-route markup | Compact form and toolbar single-select controls |
| `tooltip` | `tokens` | Shared lightweight tooltip surface, placement, layer, max size, and typography dependency | `system-ready` | Signed-off token route at `/design-system/tokens/tooltip`, with its own behavior lock, reference pack, component artifact, and canonical-rendering exception | First consumer must prove overlay containment and keyboard/focus-trigger behavior in app shell context | Shared tooltip layer |
| `choice-group` | `components` | Grouped radio/checkbox fieldset seam extracted from the `Form Template` parent | `signed-off` | Signed-off child chain at `/design-system/canonical-renderings/choice-group` and `/design-system/canonical-renderings/choice-group/:ref`, with focus and disabled-combined stress intentionally retained as parent-owned proof on `/design-system/templates/form`; legacy compatibility routes retained during migration | Prove a second governed consumer before promotion to `system-ready` | Future configuration and acknowledgement forms |
| `date-picker` | `components` | Single-date and range picker seam extracted from the `Form Template` parent | `signed-off` | Signed-off `DTPR-*` launcher and dedicated render states on `/design-system/canonical-renderings/date-picker` and `/design-system/canonical-renderings/date-picker/:ref`, plus parent-hosted proof on `/design-system/templates/form` and legacy compatibility routes retained for migration | Keep nested time-picker boundaries explicit and prove a second governed consumer before promotion to `system-ready` | Future scheduling and booking child seams |
| `drawer-select` | `components` | Search-first drawer select seam extracted from the `Form Template` parent | `signed-off` | Signed-off `DSR-*` launcher and dedicated render states on `/design-system/canonical-renderings/drawer-select` and `/design-system/canonical-renderings/drawer-select/:ref`, plus parent-hosted proof on `/design-system/templates/form` and legacy compatibility routes retained for migration | Prove a second governed consumer and write any first-consumer adoption contract before promotion to `system-ready` | Future catalog and segment selection surfaces |
| `time-picker` | `components` | Quick hour/minute picker extracted from the `Form Template` parent | `signed-off` | Signed-off `TPR-*` launcher and dedicated render states on `/design-system/canonical-renderings/time-picker` and `/design-system/canonical-renderings/time-picker/:ref`, plus explicit close-button proof and parent-hosted proof on `/design-system/templates/form`; legacy compatibility routes retained for migration | Prove a second governed consumer before promotion to `system-ready` | Future scheduling and range-with-time child seams |
| `list-record-card` | `components` | Selectable summary card extracted from the `List Page` parent template | `signed-off` | Signed-off `LRC-*` canonical set on `/design-system/canonical-renderings/list-record-card` and `/design-system/canonical-renderings/list-record-card/:ref`, including focus-visible proof, plus repeated card anatomy proven through `/design-system/templates/list-page` | Prove a second governed consumer before promotion to `system-ready` | Future list-style page families |
| `list-detail-panel` | `components` | Open detail surface extracted from the `List Page` parent template | `signed-off` | Signed-off `LDP-*` canonical set on `/design-system/canonicals/list-detail-panel` and the open detail anatomy proven through `/design-system/templates/list-page` | Prove a second governed consumer before promotion to `system-ready` | Future list-style page families |
| `list-detail-split-layout` | `components` | Master-detail lane relationship extracted from the `List Page` parent template | `signed-off` | Signed-off `LDSL-*` canonical set on `/design-system/canonicals/list-detail-split-layout` and the split shell proven through `/design-system/templates/list-page` | Prove a second governed consumer before promotion to `system-ready` | Future list-style page families |
| `list-detail-section-index` | `components` | Compact section index for moving between multiple selected-record detail aspects inside a list drawer | `adopted` | Signed-off indexed drawer variation on `/design-system/templates/list-page?drawerVariant=indexed`, documented through the behavior lock, reference pack, verification checklist, and `ListDetailSectionIndex` component seam, with first app use in root-admin root users | Prove a second governed consumer and decide whether additional section payload presets belong in the seam API before broadening beyond list detail drawers | Root admin root-users detail drawer |
| `floating-tab-header` | `components` | Floating secondary tab header with counter cards, overflow paging, subtabs, category switching, attention states, and optional content collapse | `signed-off` | Signed-off `FTH-R-001` through `FTH-R-024` reference pack now targets `/design-system/canonical-renderings/floating-tab-header` and `/design-system/canonical-renderings/floating-tab-header/:ref`; shared `renderFloatingTabHeader` and `mountFloatingTabHeader` seams remain in `/design-system/assets/floatingTabHeader.mjs`; executable canonical proof lives in `tests/visual/designSystem/canonicals/navigation/floatingTabHeaderCanonical.spec.ts` | Write a first-consumer adoption contract and real-app parity proof before any app-consumption claim; prove a second governed consumer before promotion to `system-ready` | Future list, board, and project workspaces |
| `menu` | `patterns` | Context-nav mobile More menu | `signed-off` | `/design-system` bottom-nav overflow affordance | Context-nav exploration/canonical extraction plus browser-reviewed layering and mobile interaction verification | Root admin shell mobile overflow menu |
| `drawer` | `patterns` | Filter category drawer | `signed-off` | `/design-system` filter panel | Fold filter-specific evidence into the dedicated drawer pattern and verification chain before first real consumer adoption | Shared collection filter panel |
| `selection-list` | `patterns` | Filter options secondary panel and searchable option list | `signed-off` | `/design-system` filter options flow | Pattern note plus search/no-results/selection-state verification | Shared collection filtering |
| `drawer` | `patterns` | Context-nav drawer shell | `signed-off` | `/design-system` signed-off `CDR-*` chassis states and inherited first-consumer drawer path | Prove a second real drawer consumer before shared primitive extraction | Shell-attached utility drawers |
| `display-settings` | `patterns` | Display settings payload controls | `signed-off` | Signed-off `DSR-*` payload states on `/design-system/canonical-renderings/display-settings` inside the signed-off context-nav drawer, with first app subset sign-off recorded in the downstream adoption contract | Prove a second governed consumer before promotion to `system-ready` | User preferences surfaces |
| `async-activity-drawer` | `components` | Persistent shell drawer for background job activity | `signed-off` | Signed-off shared render/controller seam in `src/frontend/designSystem/assets/asyncActivityDrawer.mjs`, consumed by `/design-system/templates/page-shell`, with generated `AADR-*` canonicals under `/design-system/canonical-renderings/async-activity-drawer` | Write a first workspace/app shell adoption contract and wire a real job feed before app adoption; prove a second governed shell consumer before `system-ready` | Workspace/app shell background jobs |
| `conversation-panel` | `patterns` | Reusable shell-attached conversation surface with history, transcript, composer tools, message actions, responsive panel framing, and packet/action extension points | `system-ready` | Shared render/controller/config seam in `/design-system/assets/conversationPanel.mjs`; shared style seam in `/design-system/assets/conversationPanel.css`; first configured canonical family is `build-work-panel`; root-admin consumes the seam with protected harness chat APIs and live assistant runtime evidence | Add a second non-Build configuration before broader promotion | Build harness, Support, team chat, future conversation surfaces |
| `build-work-panel` | `patterns` | Build-specific configuration of the conversation panel with Reporting/Support/Build toolbar, Build chat copy, Product Discovery packet PDF journey, and recoverable chat history management | `system-ready` | Signed-off pattern direction on `/design-system/patterns/build-work-panel-demo`; consumes shared `conversationPanel` render/controller/style seams; canonical routes `BWP-R-001` through `BWP-R-022` under `/design-system/canonical-renderings/build-work-panel/*`; Playwright canonical coverage in `tests/visual/designSystem/canonicals/shell/buildWorkPanel.spec.ts`; root-admin adoption coverage in `tests/visual/app/rootAdminShell/rootAdminShellParity.spec.ts`; live runtime evidence records protected harness chat, OpenAI-backed assistant response, and persisted usage attempts | Keep Reporting and Support inactive until their product flows are approved; prove a second governed consumer before broadening the primitive | Root-admin shell Build panel |
| `kanban-column` | `patterns` | Trello-like board seam with drawer-managed visible columns, insertion-line column creation, bottom add-card draft controls, recoverable archived columns, drag/drop, and non-drag card movement | `signed-off` | Signed-off shared render/controller seam in `src/frontend/designSystem/assets/kanbanColumnSeam.mjs`, consumed by `/design-system/patterns/kanban-column` and generated `KCR-001` through `KCR-017` canonical routes under `/design-system/canonical-renderings/kanban-column/*`, with behavior lock, reference pack, component seam artifact, and Playwright coverage for add-column, add-card, hidden/archive/restore behavior, drag/drop, non-drag movement result, RTL, magnification, display strain, and mobile horizontal scrolling | Write a first-consumer adoption contract and product integration plan before app adoption | Future board-style workflow surfaces |

## Recommended Promotion Order

1. `top-nav` family
2. `sub-nav`, `breadcrumb`, and `search-shell`
3. `context-nav` family
4. `drawer` family
5. `dialog` and `selection-list` family
6. `display-settings`

This order favors app-shell primitives first so the real application can start
consuming stable chrome before deeper preference panels and configurators.

## Required Artifact Chain For Each Row

Before promoting any row from `signed-off` to `system-ready`, create or refresh:

1. a principle note if the rule is enduring and not already captured
2. a pattern artifact
3. a verification checklist
4. a component artifact when the implementation seam is stable enough
5. an adoption note when real app usage begins

## First Suggested Pattern Set

- `navigation-shell-pattern.md` authored
- `sub-nav-row-pattern.md`
- `breadcrumb-pattern.md`
- `search-shell-pattern.md`
- `drawer-pattern.md`
- `display-settings-pattern.md`
- `dialog-selection-pattern.md`

## Suggested Template Set

- `launcher-template.md`

## Review Cadence

- update this inventory whenever a sign-off changes status
- move artifacts back to `needs-review` when rendered drift appears
- do not mark an artifact `adopted` without naming the consuming application surface
