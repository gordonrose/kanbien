# Harness Platform Boundary Map Decision

This folder holds an inspected artifact whose long-term owner is genuinely
unclear.

The boundary map describes a possible reusable harness platform that could
eventually map repo-specific working rules and then guide future work in that
repo. That makes it more than ordinary history, but it is not current source
truth for Kanbien's active harness.

Decision needed before promotion:

- `shared-governance-kernel` if it becomes reusable harness-platform boundary
  law.
- `discovery-harness` if it becomes a Product Discovery or Technical Steering
  packet for a future harness product.
- `archive/history` if it remains an exploratory draft after useful decisions
  are extracted elsewhere.
