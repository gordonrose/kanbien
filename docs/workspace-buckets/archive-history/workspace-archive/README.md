# Workspace Archive

This folder preserves older workspace archive material that was already
classified as `archive/history` before the repo bucket cleanup pass.

Current contents include historical capability-matrix material retained for
traceability. Do not treat these files as active planning authority without a
fresh reconciliation against current PRDs, capability matrices, standards, and
implementation state.
