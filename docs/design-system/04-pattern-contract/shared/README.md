# Shared Pattern Contracts

Store Layer 4 shared pattern contracts here:

```text
docs/design-system/04-pattern-contract/shared/<pattern-name>/<PatternName>-Contract.md
```

Shared contracts own stable composition, accessibility, primitive dependency,
state, data, and consumer-boundary decisions across design systems.
