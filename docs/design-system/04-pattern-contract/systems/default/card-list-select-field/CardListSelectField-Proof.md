# Default Card List Select Field Proof

| Field | Value |
| --- | --- |
| Layer | `04-pattern-contract` |
| Pattern | `card-list-select-field` |
| System | `default` |
| Status | `review-ready` |
| Rendered view | `/design-system/default/patterns/card-list-select-field` |

## Proof Summary

The default proof composes `field-row-control` and `card-list-select` using signed tokens consumed through those primitives.

Review controls exercise field state, card-list variant, option text pressure, column count, constrained width, direction, and theme without adding product validation or app-specific persistence.
