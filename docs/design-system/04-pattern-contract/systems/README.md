# Pattern System Proofs

Store Layer 4 system proofs here:

```text
docs/design-system/04-pattern-contract/systems/<system-key>/<pattern-name>/<PatternName>-Proof.md
```

System proofs show that one design system can render a shared pattern contract
using accepted primitives and signed tokens without changing the shared
contract.
