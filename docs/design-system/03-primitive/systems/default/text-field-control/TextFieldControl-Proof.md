# Default Text Field Control Proof

## Status

Review-ready for the `default` design system.

## Rendered Proof

View the proof route at `/design-system/default/primitives/text-field-control`.

## Evidence

The proof route renders a native single-line text input through the shared runtime seam and verifies state, label, description, focus, value typography, and frame-token wiring.
