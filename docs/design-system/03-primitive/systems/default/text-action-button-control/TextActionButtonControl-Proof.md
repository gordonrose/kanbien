# Text Action Button Control Default Proof

The `default` design system renders `text-action-button-control` at:

`/design-system/default/primitives/text-action-button-control`

The proof must show native button semantics, signed text style consumption,
signed button-frame consumption, focus visibility, minimum target sizing, and
activation event logging.
