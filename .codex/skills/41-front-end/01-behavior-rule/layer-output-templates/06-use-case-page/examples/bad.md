# Bad Use-Case Page Later-Layer Output

This output calls a one-component render proof a Layer 6 use-case page.

It has no page-family job, no feature projection shape, and no composition of
accepted components.

It also defines new component receptors and backend filter behavior inside the
behavior rule.
