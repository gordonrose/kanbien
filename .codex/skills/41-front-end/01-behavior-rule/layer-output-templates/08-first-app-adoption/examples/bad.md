# Bad First App Adoption Later-Layer Output

This is bad because it adopts before governance exists.

> Add local filter-panel markup and CSS to the users page now, then replace it with the design-system seam later.

Problems:

- Violates app-page CSS prohibition.
- Copies governed UI locally.
- Treats future seam work as optional cleanup.
