# Bad Token Later-Layer Output

This is bad because it invents token details inside the behavior rule.

> Use `--focus-ring-compact: 0 0 0 2px #4c7dff`; all compact actions must use this token.

Problems:

- Defines a token name and value before Layer 2.
- Skips existing token inventory.
- Makes a visual decision without theme evidence.
